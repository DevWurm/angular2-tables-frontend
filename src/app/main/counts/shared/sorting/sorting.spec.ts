/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Sorting} from './sorting';

describe('Sorting', () => {
  it('should create an instance', () => {
    expect(new Sorting()).toBeTruthy();
  });
});
